# Core LiveKit dependencies
livekit
livekit-agents
livekit-plugins-openai
livekit-plugins-google
livekit-plugins-cartesia
livekit-plugins-azure
livekit-plugins-deepgram
livekit-plugins-silero
livekit-plugins-noise-cancellation
livekit-plugins-liveavatar
livekit-plugins-turn-detector

# Pinecone Assistant
# Note: the [assistant] extra doesn't reliably install the plugin namespace on a fresh
# install, so pin the plugin package explicitly to guarantee pinecone_plugins.assistant.*
pinecone
pinecone-plugin-assistant

# Environment variables
python-dotenv

# Image processing
Pillow
